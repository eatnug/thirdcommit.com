/**
 * Presentation Layer - Pure Utilities
 *
 * Framework-agnostic utilities that can be used across different frameworks
 */

export { cn } from './cn';
export { debounce } from './debounce';
export { generateId } from './id';
export { getLocalStorage, setLocalStorage } from './local-storage';
